name: minio
description: A Helm Chart for MinIO as an object storage for SWOOP API (https://github.com/Element84/swoop)
version: 0.1.0
apiVersion: v2
appVersion: "0.1.0"
keywords:
  - minio
sources:
home:
# dependencies:
# - name: local-path-provisioner
#   condition: local-path-provisioner.enabled
#   version: "0.0.25"
#   repository: "https://element84.github.io/filmdrop-k8s-helm-charts/"
